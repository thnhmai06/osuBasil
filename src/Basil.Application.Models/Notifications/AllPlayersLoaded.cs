namespace Basil.Application.Models.Notifications;

/// <summary>Every playing player in the recipient's room finished loading the beatmap.</summary>
public sealed record AllPlayersLoaded : Notification;