namespace Bancho.Domain.Scores;

/// <summary>Ported from app/constants/score_statuses.py's SubmissionStatus (IntEnum).</summary>
public enum SubmissionStatus
{
    Failed = 0,
    Submitted = 1,
    Best = 2,
}
