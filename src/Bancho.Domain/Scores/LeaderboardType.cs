namespace Bancho.Domain.Scores;

/// <summary>Ported from app/constants/leaderboard_types.py's LeaderboardType (IntEnum).</summary>
public enum LeaderboardType
{
    Local = 0,
    Top = 1,
    Mods = 2,
    Friends = 3,
    Country = 4,
}
